---
name: viking-api
description: 解答 Viking 知识库 HTTP API 的接口参数、鉴权方式、调用示例及错误排查，当用户询问知识库 API 接口（签名鉴权、知识库/文档/切片/知识服务/文档解析/飞书集成/Pipeline 等）相关问题时使用。
version: 1.0.0
---

## 使用场景

本 skill 适用于回答 Viking 知识库 HTTP API 的一切问题，包括：
- 签名鉴权与 AK/SK 配置
- 各模块接口的请求参数、响应字段、错误码
- 多语言调用示例
- 文档解析、切片管理、检索问答、Pipeline 等能力的 API 接入

## Viking 知识库 API 助手

处理与本 Skill 相关的任何请求时，按以下顺序执行：

1. 运行本 Skill 安装路径下的 `scripts/upgrade.py`，检查并同步最新文档。
2. 优先阅读 `resources/index.md`，找到对应模块和接口的文档路径。
3. 进入对应的接口文档，阅读完整的请求/响应参数说明。

## 文档结构

`resources/index.md` 已汇总以下模块：

- **基础配置**：签名鉴权（AK/SK）、私网连接方式
- **知识库（Collection）**：创建、删除、查询、更新、检索、对话补全，以及图文/多轮/视频问答样例
- **文档（Doc）**：上传、删除、查询、更新元数据、按过滤条件检索，以及报错处理手册
- **切片（Point）**：添加、删除、查询、更新切片
- **知识服务（Service）**：对话接口、服务列表
- **下载**：文件下载与下载信息查询
- **实验版本（Pipeline）**：Pipeline 的完整 CRUD 及批量处理接口
- **Rerank 重排**：检索结果重排序

## 使用建议

- 若用户问题明确指向某个接口或模块，直接进入对应文档，无需读取全部内容。
- 遇到"即将下线"接口时，优先推荐同功能的新版接口。
- 遇到报错或异常，优先引导查阅 `resources/文档/文档处理报错处理手册.md`。
